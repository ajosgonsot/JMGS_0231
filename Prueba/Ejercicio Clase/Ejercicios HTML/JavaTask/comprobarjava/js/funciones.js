function calcular() {
    var Numero = parseInt(document.getElementByIdgetElementById("Numero").value);
    var resultado="";
    if intentos > 0) {
        intentos = intentos
    }
    if (Numero > aleatorio) {
        resultado = "Tu numero es mayor";
    }else if (Numero < aleatorio){
        resultado = "tu numero es menor"
    }else {
        resultado = "!!has acertado!!";

        document.getElementById("resul").innerHTML = resultado;
    }
}