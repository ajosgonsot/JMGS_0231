var Añadir = document.getElementById("Añadir")
var Fin = document.getElementById("Fin")
var Numero = document.getElementById("Numero")

function Span1(){
    if (Numero >= '0'){
        document.write("El mayor numero es:" + numero + " (Numero)");
    }
    else if (Numero < Numero){
        document.write("El Menor numero es:" + numero + " (Numero)");
    }
    if (Numero + Numero){
        document.write("La suma de los positivos es:" + numero+numero + " (Numero)");
    }
}